# 当前任务

当前任务：初始化 `video_capability_lab` 项目事实入口和三层协作骨架。

当前不是视频生成任务。  
当前不是 Remotion / HyperFrames / BGM 能力验证任务。  
当前最关键问题：仓库为空，缺少 GPT / Codex 可读取的项目事实入口。  
当前任务完成后，项目才能进入能力 probe 设计。

## 本轮边界

- 已确认：只创建骨架、机制、项目事实、执行标准、同步脚本、验证脚本和 tests。
- 已确认：只迁移源项目协作机制，不迁移业务事实。
- 待验证：第一轮能力 probe 先测哪个组件。
- 待验证：是否需要生成 GPT Project 上传包 zip。

## 本轮完成定义

验证通过、commit 成功、push 成功、远端 HEAD 验证成功后，本任务才可写 `completed`。
